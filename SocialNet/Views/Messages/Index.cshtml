@model IEnumerable<SocialNet.Models.Message>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ApplicationUser.UserSurname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SomeText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SentTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ApplicationUser.UserSurname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SomeText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SentTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MessageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MessageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MessageId })
        </td>
    </tr>
}

</table>

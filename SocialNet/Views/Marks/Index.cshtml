@model IEnumerable<SocialNet.Models.Mark>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ApplicationUser.UserSurname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comment.ApplicationUserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Post.Theme)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ApplicationUser.UserSurname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comment.ApplicationUserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Post.Theme)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MarkId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MarkId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MarkId })
        </td>
    </tr>
}

</table>
